/**
 * Houses frontend GraphQL types that should correspond directly to the types declared in the working GraphQL schema(s).
 * <p>
 * All entities (object types) declared in gmodel package should be immutable and have no outside dependencies.
 * 
 * @author jpk
 */
package com.tll.mcorpus.gmodel;