/**
 * Houses domain model types extended from <code>com.tll.mcorpus.db</code> package
 * to accommodate the web/frontend layer.
 * <p>
 * We do this to enforce a strict separation of layers so we have good modularity 
 * making for less brittle code.
 * 
 * @author jpk
 */
package com.tll.mcorpus.dmodel;